Dart follows `analysis_options.yaml` and `flutter_lints`; use records, switch expressions, and collection-if/for. Prefer `StatelessWidget`; private helpers/constants use underscore prefix. Constraint classes live under `lib/getsomepuzzle/constraints` and tests under `test`. Add ARB strings to `app_en.arb` with metadata and translations to `app_fr.arb`/`app_es.arb`, then run `flutter gen-l10n`. Use localized constraint registry helpers. Puzzle.addConstraint merges repeated LT/SZ keys. Group traversals use 4-connectivity and cached group results. Keep matching `docs/dev` pages synchronized.