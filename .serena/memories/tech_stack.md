- Language: Dart 3 with Flutter.
- Package manager: pub; `pubspec.yaml` and lockfile committed.
- Key dependencies: collection, intl, flutter_localizations, shared_preferences, url_launcher, path_provider, file_picker, share_plus, font_awesome_flutter, unicons, logging, wakelock_plus, window_manager, flutter_md.
- Localization: ARB files in `lib/l10n/`, generated through `flutter gen-l10n` using `l10n.yaml`.
- Tests: `flutter_test`; CI runs `flutter test`.
- Platforms: Android, iOS, Linux, macOS, Windows, web.