Done criteria: `rtk flutter analyze` reports zero issues; `rtk flutter test` passes; after ARB changes run `rtk flutter gen-l10n` and keep generated bindings synchronized; update relevant docs/dev pages for behavior changes; generated constraint icon changes require the focused integration capture command; `rtk git status` contains only intended changes and unrelated user work remains untouched.