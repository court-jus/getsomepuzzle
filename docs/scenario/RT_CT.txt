background bottomBarBg
dialog "Welcome!" "Let's solve a puzzle together"
wait 2000
loadState v2_12_3x5_000010002000200_CT:1.1;RT:0.0;CT:0.2;CT:2.3;RT:3.2;RT:1.1_1:222211112212211_33_scenario:classic
wait 500
background transparent
# CT:0.2
mouseTo CT:0.2
wait 200
dialog "Column Transition" "This column must contain exactly 2 colour changes."
wait 3000
dialog
wait 500
mouse 0,4
wait 500
dialog "..." "The bottom cell is already white."
wait 3000
mouse 0,0
wait 100
dialog "..." "So this top cell must be white too."
wait 3000
dialog
wait 200
setValue 0,0,white
wait 1000
# RT:0.0
mouseTo RT:0.0
wait 200
dialog "Row Transition" "This row must contain exactly 0 colour changes."
wait 3000
dialog
wait 500
mouse 0,0
wait 500
dialog "..." "This row already has a white cell — and it allows no change."
wait 3000
mouse 2,0
wait 100
dialog "..." "So every other cell must be white."
wait 3000
dialog
wait 200
setValue 2,0,white
wait 100
mouse 1,0
wait 100
setValue 1,0,white
wait 1000
# CT:1.1
mouseTo CT:1.1
wait 200
dialog "Column Transition" "This column must contain exactly 1 colour change."
wait 3000
dialog
wait 500
mouse 1,1
wait 500
dialog "..." "The middle cell is already black."
wait 3000
mouse 1,4
wait 100
dialog "..." "So the cells below must be black to keep a single change."
wait 3000
dialog
wait 200
setValue 1,4,black
wait 100
mouse 1,2
wait 100
setValue 1,2,black
wait 100
mouse 1,3
wait 100
setValue 1,3,black
wait 1000
# CT:2.3
mouseTo CT:2.3
wait 200
dialog "Column Transition" "This column must contain exactly 3 colour changes."
wait 3000
dialog
wait 500
mouse 2,2
wait 500
dialog "..." "The middle cell is already white."
wait 3000
mouse 2,4
wait 100
dialog "..." "So this cell must be black to reach three changes."
wait 3000
dialog
wait 200
setValue 2,4,black
wait 1000
# RT:3.2
mouseTo RT:3.2
wait 200
dialog "Row Transition" "This row must contain exactly 2 colour changes."
wait 3000
dialog
wait 500
mouse 1,3
wait 500
dialog "..." "The middle cell is already black."
wait 3000
mouse 0,3
wait 100
dialog "..." "So the cells on each side must be white."
wait 3000
dialog
wait 200
setValue 0,3,white
wait 100
mouse 2,3
wait 100
setValue 2,3,white
wait 1000
# CT:2.3
mouseTo CT:2.3
wait 200
dialog "Column Transition" "This column must contain exactly 3 colour changes."
wait 3000
dialog
wait 500
mouse 2,2
wait 500
dialog "..." "The middle cell is already white."
wait 3000
mouse 2,1
wait 100
dialog "..." "So this cell must be black to reach three changes."
wait 3000
dialog
wait 200
setValue 2,1,black
wait 1000
# RT:1.1
mouseTo RT:1.1
wait 200
dialog "Row Transition" "This row must contain exactly 1 colour change."
wait 3000
dialog
wait 500
mouse 1,1
wait 500
mouse 2,1
wait 500
dialog "..." "The row already has two black cells."
wait 3000
mouse 0,1
wait 100
dialog "..." "So this cell must be white to keep a single change."
wait 3000
dialog
wait 200
setValue 0,1,white
wait 1000
# CT:0.2
mouseTo CT:0.2
wait 200
dialog "Column Transition" "This column must contain exactly 2 colour changes."
wait 3000
dialog
wait 500
mouse 0,0
wait 500
mouse 0,1
wait 500
mouse 0,3
wait 500
mouse 0,4
wait 500
dialog "..." "The column already has four white cells."
wait 3000
mouse 0,2
wait 100
dialog "..." "So this cell must be black to create two changes."
wait 3000
dialog
wait 200
setValue 0,2,black
wait 1000
# End
dialog
wait 3000
background bottomBarBg
dialog "Puzzle solved!" "Go try it by yourself at leveque.cc/getsomepuzzle"
wait 5000
