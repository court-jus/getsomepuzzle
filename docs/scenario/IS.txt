background bottomBarBg
dialog "Welcome!" "Let's solve a puzzle together"
wait 2000
loadState v2_12_4x5_10000000000000120000_CC:3.1.4;DF:6.down;DF:9.right;NC:0.2.1;NC:12.2.0;CC:1.2.3;IS:2;RC:0.1.2_1:12211221121111121111_17_scenario:classic
wait 500
background transparent
# CC:3.1.4
mouseTo CC:3.1.4
wait 200
dialog "Column Count" "This column must contain exactly 4 black cells."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 3,3
# wait 500
dialog "..." "One cell in this column is already white."
wait 3000
dialog "..." "So all the other cells must be black."
wait 3000
dialog
wait 200
mouse 3,0
wait 100
setValue 3,0,black
wait 1000
mouse 3,1
wait 100
setValue 3,1,black
wait 1000
mouse 3,2
wait 100
setValue 3,2,black
wait 1000
mouse 3,4
wait 100
setValue 3,4,black
wait 1000
# NC:12.2.0
mouseTo NC:12.2.0
wait 200
dialog "Neighbour Count" "This cell must have no white neighbours at all."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 0,3
# wait 500
dialog "..." "It has exactly three neighbouring cells."
wait 3000
dialog "..." "So all three neighbours must be black."
wait 3000
dialog
wait 200
mouse 0,2
wait 100
setValue 0,2,black
wait 1000
mouse 0,4
wait 100
setValue 0,4,black
wait 1000
mouse 1,3
wait 100
setValue 1,3,black
wait 1000
# IS:2
mouseTo IS:2
wait 200
dialog "Islands" "White groups must never touch each other, not even diagonally."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 3,3
# wait 500
dialog "..." "This cell is already a lone white island."
wait 3000
dialog "..." "So the cells touching its corners cannot be white — they must be black."
wait 3000
dialog
wait 200
mouse 2,2
wait 100
setValue 2,2,black
wait 1000
mouse 2,4
wait 100
setValue 2,4,black
wait 1000
# DF:6.down
mouseTo DF:6.down
wait 200
dialog "Different From" "This cell must be different from its neighbour."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 2,2
# wait 500
dialog "..." "This neighbour is already black."
wait 3000
dialog "..." "So this cell must be white."
wait 3000
dialog
wait 200
mouse 2,1
wait 100
setValue 2,1,white
wait 1000
# DF:9.right
mouseTo DF:9.right
wait 200
dialog "Different From" "This cell must be different from its neighbour."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 2,2
# wait 500
dialog "..." "This neighbour is already black."
wait 3000
dialog "..." "So this cell must be white."
wait 3000
dialog
wait 200
mouse 1,2
wait 100
setValue 1,2,white
wait 1000
# IS:2
mouseTo IS:2
wait 200
dialog "Islands" "White groups must never touch each other, not even diagonally."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 2,1
# wait 500
# mouse 1,2
# wait 500
dialog "..." "These two white cells touch only at a corner."
wait 3000
dialog "..." "So the bridging cell must be white to join them, and the corner cell must stay black."
wait 3000
dialog
wait 200
mouse 0,3
wait 100
setValue 0,3,black
wait 1000
mouse 1,1
wait 100
setValue 1,1,white
wait 1000
# RC:0.1.2
mouseTo RC:0.1.2
wait 200
dialog "Row Count" "This row must contain exactly 2 black cells."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 0,0
# wait 500
# mouse 3,0
# wait 500
dialog "..." "It already has its two black cells."
wait 3000
dialog "..." "So the remaining cells must be white."
wait 3000
dialog
wait 200
mouse 1,0
wait 100
setValue 1,0,white
wait 1000
mouse 2,0
wait 100
setValue 2,0,white
wait 1000
# NC:0.2.1
mouseTo NC:0.2.1
wait 200
dialog "Neighbour Count" "This cell must have exactly one white neighbour."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 1,0
# wait 500
dialog "..." "One of its neighbours is already white."
wait 3000
dialog "..." "So the other neighbour must be black."
wait 3000
dialog
wait 200
mouse 0,1
wait 100
setValue 0,1,black
wait 1000
# CC:1.2.3
mouseTo CC:1.2.3
wait 200
dialog "Column Count" "This column must contain exactly 3 white cells."
wait 3000
dialog
wait 500
# TODO: Mouse to relevant already-set cells
# mouse 1,0
# wait 500
# mouse 1,1
# wait 500
# mouse 1,2
# wait 500
dialog "..." "It already has its three white cells."
wait 3000
dialog "..." "So the remaining cell must be black."
wait 3000
dialog
wait 200
mouse 1,4
wait 100
setValue 1,4,black
wait 1000
# End
dialog
wait 3000
background bottomBarBg
dialog "Puzzle solved!" "Go try it by yourself at leveque.cc/getsomepuzzle"
wait 2000
dialog
