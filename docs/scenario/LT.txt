background bottomBarBg
dialog "Welcome!" "Let's solve a puzzle together"
wait 2000
loadState v2_12_3x4_000000000022_LT:B.3.10.11.5;LT:A.9.4_1:222212112122_31_scenario:classic
wait 500
background transparent
# LT:B.3.10.11.5
mouseTo LT:B.3.10.11.5
wait 200
dialog "Letter Group" "The cells of this letter must share one colour and form a single connected group."
wait 3000
dialog
wait 500
mouse 1,3
wait 500
mouse 2,3
wait 500
dialog "..." "These cells of the letter are already white."
wait 3000
mouse 0,1
wait 100
dialog "..." "So the rest of the letter must be white — and the cells between must stay black."
wait 3000
dialog
wait 200
setValue 0,1,white
wait 100
mouse 2,1
wait 100
setValue 2,1,white
wait 100
mouse 1,1
wait 100
setValue 1,1,black
wait 100
mouse 0,3
wait 100
setValue 0,3,black
wait 1000
# LT:A.9.4
mouseTo LT:A.9.4
wait 200
dialog "Letter Group" "The cells of this letter must share one colour and form a single connected group."
wait 3000
dialog
wait 500
mouse 1,1
wait 500
mouse 0,3
wait 500
dialog "..." "These two cells of the letter are already black."
wait 3000
mouse 0,2
wait 100
dialog "..." "So the cells between them must be black to connect the letter."
wait 3000
dialog
wait 200
setValue 0,2,black
wait 100
mouse 1,2
wait 100
setValue 1,2,black
wait 1000
# LT:B.3.10.11.5
mouseTo LT:B.3.10.11.5
wait 200
dialog "Letter Group" "The cells of this letter must share one colour and form a single connected group."
wait 3000
dialog
wait 500
mouse 0,1
wait 500
mouse 2,1
wait 500
mouse 1,3
wait 500
mouse 2,3
wait 500
dialog "..." "The letter cells are all white."
wait 3000
mouse 0,0
wait 100
dialog "..." "So the cells around them must be white too, to connect the letter into one group."
wait 3000
dialog
wait 200
setValue 0,0,white
wait 100
mouse 1,0
wait 100
setValue 1,0,white
wait 100
mouse 2,0
wait 100
setValue 2,0,white
wait 100
mouse 2,2
wait 100
setValue 2,2,white
wait 1000
# End
dialog
wait 3000
background bottomBarBg
dialog "Puzzle solved!" "Go try it by yourself at leveque.cc/getsomepuzzle"
wait 5000
