background bottomBarBg
dialog "Welcome!" "Let's solve a puzzle together"
wait 2000
loadState v2_12_3x3_000100020_NC:3.2.2;NC:4.1.3;NC:5.2.0_1:211111221_5_scenario:classic
wait 500
background transparent
# NC:4.1.3
mouseTo NC:4.1.3
wait 200
dialog "Neighbour Count" "This cell must have exactly 3 black neighbours."
wait 3000
dialog
wait 500
mouse 0,1
wait 500
dialog "..." "One neighbour is already black — and that one is already white."
wait 3000
mouse 1,0
wait 100
dialog "..." "So the two remaining neighbours must both be black."
wait 3000
dialog
wait 200
setValue 1,0,black
wait 100
mouse 2,1
wait 100
setValue 2,1,black
wait 1000
# NC:5.2.0
mouseTo NC:5.2.0
wait 200
dialog "Neighbour Count" "This cell must have exactly 0 white neighbours."
wait 3000
dialog
wait 500
mouse 2,1
wait 500
dialog "..." "This cell has just been filled — it allows no white around it."
wait 3000
mouse 2,0
wait 100
dialog "..." "So every neighbour must be black."
wait 3000
dialog
wait 200
setValue 2,0,black
wait 100
mouse 2,2
wait 100
setValue 2,2,black
wait 100
mouse 1,1
wait 100
setValue 1,1,black
wait 1000
# NC:3.2.2
mouseTo NC:3.2.2
wait 200
dialog "Neighbour Count" "This cell must have exactly 2 white neighbours."
wait 3000
dialog
wait 500
mouse 1,1
wait 500
dialog "..." "One neighbour is already black — and it needs two white ones."
wait 3000
mouse 0,0
wait 100
dialog "..." "So these two cells must be white."
wait 3000
dialog
wait 200
setValue 0,0,white
wait 100
mouse 0,2
wait 100
setValue 0,2,white
wait 1000
# End
dialog
wait 3000
background bottomBarBg
dialog "Puzzle solved!" "Go try it by yourself at leveque.cc/getsomepuzzle"
wait 5000
