loadState v2_12_3x6_000000000000000020_DF:3.right;NC:0.2.1;NC:15.1.0;CC:0.2.1;CC:2.2.1;JR:4.21;RC:2.1.1;RC:3.1.2_1:121121122121221121_23_scenario:classic
wait 500
# --- Group 1: NC:15.1.0 — cell (5,1) must be white ---
mouseTo NC:15.1.0
wait 200
dialog "Neighbor Count" "Cell (6,1) must have exactly 0 black neighbours.\nIts neighbours are (5,1) above and (6,2) to the right.\nSince (6,2) is already white, (5,1) cannot be black — it must be white."
wait 8000
dialog
wait 200
mouse 0,4
wait 100
setValue 0,4,white
wait 100
# --- Group 2: CC:0.2.1 — column 1's only white cell is already placed ---
mouseTo CC:0.2.1
wait 200
dialog "Column Count" "Column 1 must contain exactly 1 white cell.\nCell (5,1) in this column was already set to white by the neighbour-count constraint.\nTherefore no other cell in column 1 can be white.\nThis forces (1,1) and (2,1) to be black."
wait 8000
dialog
wait 200
mouse 0,0
wait 100
setValue 0,0,black
wait 100
mouse 0,1
wait 100
setValue 0,1,black
wait 100
# --- Group 3: DF:3.right — (2,1) ≠ (2,2) ---
mouseTo DF:3.right
wait 200
dialog "Different From" "Cell (2,1) must be different from its right neighbour (2,2).\nWe just determined that (2,1) is black, so (2,2) cannot also be black — it must be white."
wait 8000
dialog
wait 200
mouse 1,1
wait 100
setValue 1,1,white
wait 100
# --- Group 4: NC:0.2.1 — cell (1,1) needs 1 white neighbour ---
mouseTo NC:0.2.1
wait 200
dialog "Neighbor Count" "Cell (1,1) must have exactly 1 white neighbour.\nIts neighbours are (1,2) to the right and (2,1) below.\nSince (2,1) is black, the single white neighbour must be (1,2).\nTherefore (1,2) must be white."
wait 8000
dialog
wait 200
mouse 1,0
wait 100
setValue 1,0,white
wait 100
# --- Group 5: CC:0.2.1 continues — remaining cells in column 1 ---
mouseTo CC:0.2.1
wait 200
dialog "Column Count" "Continuing with column 1 — it can only have 1 white cell, which is already placed at (5,1).\nThe remaining free cells (3,1), (4,1), and (6,1) must all be black."
wait 8000
dialog
wait 200
mouse 0,2
wait 100
setValue 0,2,black
wait 100
mouse 0,3
wait 100
setValue 0,3,black
wait 100
mouse 0,5
wait 100
setValue 0,5,black
wait 100
# --- Group 6: RC:2.1.1 — row 3 has exactly 1 black cell ---
mouseTo RC:2.1.1
wait 200
dialog "Row Count" "Row 3 must contain exactly 1 black cell.\nCell (3,1) is already black, so the other cells in row 3 — (3,2) and (3,3) — cannot be black.\nThey must both be white."
wait 8000
dialog
wait 200
mouse 1,2
wait 100
setValue 1,2,white
wait 100
mouse 2,2
wait 100
setValue 2,2,white
wait 100
# --- Group 7: CC:2.2.1 — column 3's only white cell is now placed ---
mouseTo CC:2.2.1
wait 200
dialog "Column Count" "Column 3 must contain exactly 1 white cell.\nCell (3,3) was just set to white, fulfilling that requirement.\nConsequently all other cells in column 3 — (1,3), (2,3), (4,3), (5,3), and (6,3) — cannot be white.\nThey must all be black."
wait 8000
dialog
wait 200
mouse 2,0
wait 100
setValue 2,0,black
wait 100
mouse 2,1
wait 100
setValue 2,1,black
wait 100
mouse 2,3
wait 100
setValue 2,3,black
wait 100
mouse 2,4
wait 100
setValue 2,4,black
wait 100
mouse 2,5
wait 100
setValue 2,5,black
wait 100
# --- Group 8: JR:4.21 — more white than black in row 5 ---
mouseTo JR:4.21
wait 200
dialog "Majority" "In row 5, white must outnumber black.\nCurrently (5,1) is white and (5,3) is black, a 1-1 tie.\nTo give white the majority, (5,2) must be white."
wait 8000
dialog
wait 200
mouse 1,4
wait 100
setValue 1,4,white
wait 100
# --- Group 9: RC:3.1.2 — row 4 has exactly 2 black cells ---
mouseTo RC:3.1.2
wait 200
dialog "Row Count" "Row 4 must contain exactly 2 black cells.\nCells (4,1) and (4,3) are already black from earlier deductions.\nThat reaches the limit, so (4,2) cannot be black — it must be white."
wait 8000
dialog
wait 200
mouse 1,3
wait 100
setValue 1,3,white
wait 100
# --- Finished ---
mouseTo hint
wait 300
dialog "Solved!" "The puzzle is complete!\nAll constraints are satisfied."
wait 3000
dialog
