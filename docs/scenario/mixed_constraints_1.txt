loadState v2_12_3x6_000000000000000020_DF:3.right;NC:0.2.1;NC:15.1.0;CC:0.2.1;CC:2.2.1;JR:4.21;RC:2.1.1;RC:3.1.2_1:121121122121221121_23_scenario:classic
wait 500
# --- Group 1: NC:15.1.0 — cell (5,0) has at most 0 black neighbours ---
mouseTo NC:15.1.0
wait 200
dialog "Neighbour Count" "Cell (5,0) must have at most 0 black neighbours. Since (4,0) is directly adjacent to (5,0), making (4,0) black would give (5,0) a black neighbour — violating the constraint. Therefore (4,0) must be white."
wait 6300
dialog
wait 200
mouse 0,4
wait 100
setValue 0,4,white
wait 100
# --- Group 2: CC:0.2.1 — column 0 must have exactly 1 white ---
mouseTo CC:0.2.1
wait 200
dialog "Column Count" "Column 0 must contain exactly 1 white cell. Cell (4,0) was just set to white, so the remaining cells in this column — (0,0), (1,0), (2,0), (3,0), (5,0) — must all be black to stay at exactly 1 white."
wait 7200
dialog
wait 200
mouse 0,0
wait 100
setValue 0,0,black
wait 100
mouse 0,1
wait 100
setValue 0,1,black
wait 100
mouse 0,2
wait 100
setValue 0,2,black
wait 100
mouse 0,3
wait 100
setValue 0,3,black
wait 100
mouse 0,5
wait 100
setValue 0,5,black
wait 100
# --- Group 3: DF:3.right — (1,1) and (1,2) must differ ---
mouseTo DF:3.right
wait 200
dialog "Different From" "Cells (1,1) and (1,2) must be different colours. Other constraints force (1,2) to be black, so (1,1) cannot be black — it must be white."
wait 5400
dialog
wait 200
mouse 1,1
wait 100
setValue 1,1,white
wait 100
# --- Group 4: NC:0.2.1 — neighbour count for cell (0,0) ---
mouseTo NC:0.2.1
wait 200
dialog "Neighbour Count" "Cell (0,0) must have exactly 2 white neighbours. With (1,1) already white and (1,0) black, the only remaining neighbour of (0,0) is (0,1) — which must be white to reach the count of 2."
wait 6600
dialog
wait 200
mouse 1,0
wait 100
setValue 1,0,white
wait 100
# --- Group 5: RC:2.1.1 — row 2 must have exactly 1 black ---
mouseTo RC:2.1.1
wait 200
dialog "Row Count" "Row 2 must contain exactly 1 black cell. Cell (2,0) is already black, so the other cells in this row — (2,1) and (2,2) — cannot be black. They must both be white."
wait 6000
dialog
wait 200
mouse 1,2
wait 100
setValue 1,2,white
wait 100
mouse 2,2
wait 100
setValue 2,2,white
wait 100
# --- Group 6: CC:2.2.1 — column 2 must have exactly 1 white ---
mouseTo CC:2.2.1
wait 200
dialog "Column Count" "Column 2 must contain exactly 1 white cell. Cell (2,2) is already white, so the remaining cells in column 2 — (0,2), (1,2), (3,2), (4,2), (5,2) — must all be black."
wait 6600
dialog
wait 200
mouse 2,0
wait 100
setValue 2,0,black
wait 100
mouse 2,1
wait 100
setValue 2,1,black
wait 100
mouse 2,3
wait 100
setValue 2,3,black
wait 100
mouse 2,4
wait 100
setValue 2,4,black
wait 100
mouse 2,5
wait 100
setValue 2,5,black
wait 100
# --- Group 7: JR:4.21 — white majority in row 4 ---
mouseTo JR:4.21
wait 200
dialog "Majority" "Row 4 must have more white cells than black cells. Currently (4,0) is white and (4,2) is black. To give white the majority, (4,1) must also be white."
wait 6000
dialog
wait 200
mouse 1,4
wait 100
setValue 1,4,white
wait 100
# --- Group 8: RC:3.1.2 — row 3 must have exactly 1 white ---
mouseTo RC:3.1.2
wait 200
dialog "Row Count" "Row 3 must contain exactly 1 white cell. Cells (3,0) and (3,2) are already black, so (3,1) must be white to provide the single white cell in this row."
wait 6300
dialog
wait 200
mouse 1,3
wait 100
setValue 1,3,white
wait 100
# --- Finished ---
mouseTo hint
wait 300
dialog "Solved!" "The puzzle is complete!\nAll constraints are satisfied."
wait 3000
dialog
